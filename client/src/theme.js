import { createMuiTheme } from '@material-ui/core/styles'
const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#1DB954',
    },
    background: '#1f1b1b',
    text: {
      primary: '#fff'
    }
  },
  darkGray: '#fff',
})
export default theme